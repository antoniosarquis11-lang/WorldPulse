WorldPulse version 1.5 — Netlify-ready static build

Updates from version 1.3:
- Removed the image logo from the home page navigation.
- Removed the image logo from the sidebar/navigation layout.
- Removed the unused logo asset from the build.
- Fixed the home page scroll behavior so the “How it works” button scrolls to the tour section without trapping the page; users can scroll back up normally.

Deploy by uploading these files to Netlify or dragging this folder into Netlify Drop.
